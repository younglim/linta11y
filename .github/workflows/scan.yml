name: 🕵️‍♀️ Remote Accessibility Scanner

on:
  workflow_dispatch:
    inputs:
      target_repo_url:
        description: 'GitHub Repo URL to scan (e.g. https://github.com/user/repo)'
        required: true
        type: string

jobs:
  remote-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Host Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # FIX 1: Install TypeScript Parsers
      # We added @typescript-eslint/parser and related plugins to handle .tsx files
      - name: Install Scanner Engine
        run: |
          npm install eslint \
            eslint-plugin-jsx-a11y \
            eslint-plugin-react \
            @typescript-eslint/parser \
            @typescript-eslint/eslint-plugin \
            typescript

      - name: Clone Target Repository
        run: |
          git clone ${{ inputs.target_repo_url }} ./target-code

      # FIX 2: Update ESLint Config to use the TS Parser
      - name: Generate Configurations
        run: |
          # --- Create ESLint Config ---
          cat << 'EOF' > .eslintrc-temp.json
          {
            "root": true,
            "parser": "@typescript-eslint/parser",
            "parserOptions": {
              "ecmaVersion": 2020,
              "sourceType": "module",
              "ecmaFeatures": {
                "jsx": true
              }
            },
            "plugins": ["jsx-a11y", "react", "@typescript-eslint"],
            "extends": [
              "plugin:jsx-a11y/strict"
            ],
            "rules": {
              # React Native doesn't use standard HTML elements, so we map some rules
              # or disable ones that don't apply to native (like href validity)
              "react/jsx-uses-react": "off",
              "react/react-in-jsx-scope": "off",
              "jsx-a11y/anchor-is-valid": "off" 
            }
          }
          EOF

          # --- Create WCAG Mapper Script ---
          cat << 'EOF' > map-wcag.js
          const fs = require('fs');
          
          // Define the WCAG mapping
          const ruleMap = {
            'jsx-a11y/alt-text': 'WCAG 1.1.1 (Non-text Content)',
            'jsx-a11y/anchor-has-content': 'WCAG 2.4.4 (Link Purpose)',
            'jsx-a11y/click-events-have-key-events': 'WCAG 2.1.1 (Keyboard)',
            'jsx-a11y/no-static-element-interactions': 'WCAG 2.1.1 (Keyboard)',
            'jsx-a11y/label-has-associated-control': 'WCAG 1.3.1 (Info and Relationships)',
            'jsx-a11y/accessible-emoji': 'WCAG 1.1.1 (Non-text Content)',
            'jsx-a11y/aria-props': 'WCAG 4.1.2 (Name, Role, Value)',
            'jsx-a11y/heading-has-content': 'WCAG 1.3.1 (Info and Relationships)',
            'jsx-a11y/mouse-events-have-key-events': 'WCAG 2.1.1 (Keyboard)',
            'jsx-a11y/role-has-required-aria-props': 'WCAG 4.1.2 (Name, Role, Value)',
            'jsx-a11y/role-supports-aria-props': 'WCAG 4.1.2 (Name, Role, Value)'
          };
          
          try {
            if (!fs.existsSync('eslint-raw.json')) { 
              console.log("No raw report found."); 
              process.exit(0); 
            }
            
            const content = fs.readFileSync('eslint-raw.json', 'utf8');
            if (!content) {
               console.log("Report is empty.");
               process.exit(0);
            }

            const report = JSON.parse(content);
            
            const enriched = report.map(f => {
              // Filter out files with no messages AND parsing errors
              if (f.messages.length === 0) return null;

              // Clean up path
              f.filePath = f.filePath.split('/target-code/')[1] || f.filePath;
              
              f.messages = f.messages.map(m => {
                // Check if it's still a parsing error (should be fixed now)
                if (m.message && m.message.includes("Parsing error")) {
                   return { ...m, wcagClause: "Tool Configuration Error" };
                }
                return { 
                  ...m, 
                  wcagClause: ruleMap[m.ruleId] || 'Unmapped / Best Practice' 
                };
              });
              return f;
            }).filter(Boolean);
            
            fs.writeFileSync('accessibility-report.json', JSON.stringify(enriched, null, 2));
            console.log(`✅ Report generated. Found issues in ${enriched.length} files.`);
          } catch (e) { 
            console.error("Error processing JSON:", e); 
          }
          EOF

      - name: Execute Scan
        continue-on-error: true
        run: |
          # We explicitly tell ESLint to use the config we just made
          npx eslint ./target-code \
          --config .eslintrc-temp.json \
          --no-eslintrc \
          --ext .js,.jsx,.ts,.tsx \
          --format json \
          -o eslint-raw.json

      - name: Enrich with WCAG Data
        run: node map-wcag.js

      - name: Upload JSON Report
        uses: actions/upload-artifact@v4
        with:
          name: accessibility-report-${{ github.run_id }}
          path: accessibility-report.json
