name: 🚀 Universal A11y Scanner

on:
  workflow_dispatch:
    inputs:
      target_repo_url:
        description: 'GitHub Repo URL to scan'
        required: true
        type: string
      target_branch:
        description: 'Branch to scan (e.g. starter). Leave empty for default.'
        required: false
        type: string

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Host Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Scanner Engine
        # 1. 'npm install' installs ESLint 9 (from your package.json)
        run: |
          npm install

      - name: Clone Target Repository
        run: |
          if [ -n "${{ inputs.target_branch }}" ]; then
            echo "📍 Cloning specific branch: ${{ inputs.target_branch }}"
            git clone --depth 1 --branch ${{ inputs.target_branch }} ${{ inputs.target_repo_url }} ./target-code
          else
            echo "📍 Cloning default branch"
            git clone --depth 1 ${{ inputs.target_repo_url }} ./target-code
          fi

      - name: Run Audit
        run: node ./cli.js --oobee --debug --target ./target-code
        env:
          LINTA11Y_DEBUG: 1
          TARGET_REPO_URL: ${{ inputs.target_repo_url }}
          TARGET_BRANCH: ${{ inputs.target_branch }}

      - name: Upload Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: accessibility-reports
          path: |
            accessibility-report.json
            accessibility-report.html